#ifndef CK_ACME_H
#define CK_ACME_H

#define MAX(a,b) ((a) > (b) ? (a) : (b))
#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define AT_LEAST(a,b) ((a) = MAX(a,b))
#define AT_MOST(a,b) ((a) = MIN(a,b))

#endif // CK_ACME_H
